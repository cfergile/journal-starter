# --- Database (pieces) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=journal
DB_USER=postgres
DB_PASSWORD=postgres

# Optional full DSN (app prefers this if present)
# DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/journal

# --- Runtime ---
LOG_LEVEL=INFO
PROMETHEUS_ENABLED=true
# SENTRY_DSN=
